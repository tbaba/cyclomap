%section.tagged-courses
  %h2
    = fa_icon('map-marker')
    = @tag_name
    のコース

  %ul.list-unstyled
    - @courses.each do |course|
      %li
        %h3
          %span.title
            = link_to course.title, course
          %span.star.btn.btn-sm
            = fa_icon('star', text: course.stars_count)
