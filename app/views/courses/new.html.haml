%section
  %h2
    コースの登録

  = form_for @course, html: { role: 'form' } do |f|
    = render 'error_explanation', model: @course

    .form-group
      = f.label :title
      = f.text_field :title, class: 'form-control'

    .form-group
      = f.label :body
      = f.text_area :body, class: 'form-control'

    .form-group
      = f.label :route_lab_url
      = f.text_field :route_lab_url, class: 'form-control'

    .form-group
      = f.label :tag_list
      = f.text_field :tag_list, class: 'form-control'
      %span.help-block= ',（カンマ）区切りで入力してください'

    .form-group.button-nav
      %ul.list-unstyled
        %li= f.button fa_icon('check', text: '登録する'), type: 'submit', class: 'btn btn-primary'
